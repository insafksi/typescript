export interface HasHtmlFormat {   //l'interface pour déterminer la forme d'un objet
    htmlFormat(): string;   //une méthode pour nous créer le format html de notre facutre et elle retourne string 
 
}